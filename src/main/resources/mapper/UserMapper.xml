<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.farm404.samyang3.mapper.UserMapper">

    <insert id="insertUser" parameterType="UserVO" useGeneratedKeys="true" keyProperty="userID">
        INSERT INTO User (Username, Password, Email, FullName, Phone, Address, IsAdmin)
        VALUES (#{username}, #{password}, #{email}, #{fullName}, #{phone}, #{address}, #{isAdmin})
    </insert>
    
    <select id="selectByUsername" parameterType="String" resultType="UserVO">
        SELECT * FROM User WHERE Username = #{username}
    </select>
    
    <select id="selectByEmail" parameterType="String" resultType="UserVO">
        SELECT * FROM User WHERE Email = #{email}
    </select>
    
    <select id="selectById" parameterType="Integer" resultType="UserVO">
        SELECT * FROM User WHERE UserID = #{userID}
    </select>
    
    <update id="updateUser" parameterType="UserVO">
        UPDATE User 
        SET Email = #{email},
            FullName = #{fullName},
            Phone = #{phone},
            Address = #{address}
        WHERE UserID = #{userID}
    </update>
    
    <update id="updatePassword">
        UPDATE User 
        SET Password = #{password}
        WHERE UserID = #{userID}
    </update>
    
    <select id="selectAllUsers" resultType="UserVO">
        SELECT * FROM User 
        ORDER BY UserID DESC
    </select>
    
    <select id="countUsers" resultType="int">
        SELECT COUNT(*) FROM User
    </select>
    
    <update id="updateAdminStatus">
        UPDATE User 
        SET IsAdmin = #{isAdmin}
        WHERE UserID = #{userID}
    </update>

</mapper>